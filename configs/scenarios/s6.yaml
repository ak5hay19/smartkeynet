# configs/scenarios/s6.yaml
#
# Migration schedule: timed floor_change events per tenant cohort.
#
# Scenario mechanics live in `env/scenarios.py::build_scenario`, which is what
# `configs/default.yaml`'s `scenario:` key selects. These per-scenario files
# exist because SMARTKEYNET_BUILD_SPEC.md §3.1 lists them in the repo layout
# and §S6 requires each scenario to be "config only, no new code". They layer
# over `configs/default.yaml` -- merge, do not replace.
#
# train/eval: EVAL ONLY -- Hard Rule 8; require_trainable() refuses to train on this
scenario: S6
eval_only: true
request_source: graph
