# SmartKeyNet dependencies (PLAN.md §4 "Tech stack").
# Pin loosely for a capstone; tighten if reproducibility issues arise.

# --- env / MDP ---
gymnasium>=0.29
numpy>=1.26
networkx>=3.2

# --- agents / forecaster ---
torch>=2.2

# --- data ingestion (RT-IoT2022 feature-extraction pipeline,
#     forecaster/rt_iot_features.py + data/get_data.py) ---
pandas>=2.0
requests>=2.31
ucimlrepo>=0.0.7  # official UCI ML Repository client

# --- API facade ---
fastapi>=0.110
pydantic>=2.6
uvicorn>=0.27

# --- real primitive calls (PLAN.md §4: "real primitive calls... used where cheap") ---
cryptography>=42.0
# liboqs-python  # optional: uncomment once liboqs is built/installed locally

# --- config ---
PyYAML>=6.0

# --- dashboard (PLAN.md tech stack: "Plotly Dash or a small React dashboard") ---
dash>=2.16
plotly>=5.20

# --- dev / test ---
pytest>=8.0
pytest-cov>=5.0
